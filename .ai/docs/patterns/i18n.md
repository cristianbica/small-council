# Pattern: i18n

- Do not hard-code user-facing strings where i18n is expected.
- Use consistent key naming and reuse keys rather than duplicating.
- Preserve placeholders/interpolations exactly.
- Handle pluralization using the framework’s native mechanism.
