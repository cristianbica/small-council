<div data-controller="prompt-generator"
     data-prompt-generator-url-value="<%= council.persisted? ? generate_description_council_path(council) : generate_description_councils_path %>">

  <%= form_with model: council, class: "space-y-4" do |f| %>
    <% if council.errors.any? %>
      <div class="alert alert-error">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 stroke-current" fill="none" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <div>
          <h3 class="font-bold"><%= pluralize(council.errors.count, "error") %> prohibited this council from being saved:</h3>
          <ul class="mt-1 list-disc list-inside text-sm">
            <% council.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      </div>
    <% end %>

    <div class="form-control">
      <label class="label">
        <span class="label-text">
          Name
          <span class="text-error" aria-label="required">*</span>
        </span>
      </label>
      <%= f.text_field :name,
          class: "input input-bordered w-full #{'input-error' if council.errors[:name].any?}",
          placeholder: "e.g., Engineering Leadership Council",
          required: true,
          data: { "prompt-generator-target": "councilNameField" } %>
      <% if council.errors[:name].any? %>
        <label class="label">
          <span class="label-text-alt text-error flex items-center gap-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <%= council.errors[:name].first %>
          </span>
        </label>
      <% end %>
    </div>

    <div class="form-control">
      <div class="flex items-center justify-between">
        <label class="label">
          <span class="label-text">Description</span>
        </label>
        <button type="button"
                class="btn btn-sm btn-secondary"
                data-action="click->prompt-generator#openModal">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
          Generate with AI
        </button>
      </div>
      <%= f.text_area :description,
          class: "textarea textarea-bordered w-full #{'textarea-error' if council.errors[:description].any?}",
          rows: 3,
          placeholder: "What is this council for?",
          data: { "prompt-generator-target": "descriptionField" } %>
      <% if council.errors[:description].any? %>
        <label class="label">
          <span class="label-text-alt text-error flex items-center gap-1">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <%= council.errors[:description].first %>
          </span>
        </label>
      <% end %>
    </div>

    <div class="card-actions justify-end mt-6">
      <%= link_to "Cancel", councils_path, class: "btn btn-ghost" %>
      <%= f.submit class: "btn btn-primary" %>
    </div>
  <% end %>

  <%# Modal for AI Description Generation %>
  <dialog data-prompt-generator-target="modal" 
          class="modal"
          data-action="click->prompt-generator#clickOutside">
    <div class="modal-box">
      <h3 class="font-bold text-lg mb-4">Generate Description with AI</h3>
      
      <p class="text-sm text-base-content/70 mb-4">
        The AI will generate a compelling description for your council based on its name.
      </p>

      <div class="form-control mb-4">
        <label class="label">
          <span class="label-text">Council Name</span>
        </label>
        <input type="text" 
               data-prompt-generator-target="descriptionInput"
               class="input input-bordered w-full"
               placeholder="e.g., Engineering Leadership Council"
               required>
      </div>

      <div data-prompt-generator-target="errorMessage" 
           class="alert alert-error hidden mb-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span></span>
      </div>

      <div class="modal-action">
        <button type="button" 
                class="btn btn-ghost"
                data-action="click->prompt-generator#closeModal">
          Cancel
        </button>
        <button type="button" 
                data-prompt-generator-target="generateButton"
                data-action="click->prompt-generator#generate"
                class="btn btn-primary">
          <span data-prompt-generator-target="loadingIndicator" class="hidden loading loading-spinner loading-xs mr-2"></span>
          Generate Description
        </button>
      </div>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>

</div>
